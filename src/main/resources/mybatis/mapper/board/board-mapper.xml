<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">
    <select id="selectCategoryByName" resultType="com.end2end.spring.board.dto.BoardcategoryDTO">
        SELECT * FROM BOARD_CATEGORY WHERE NAME = #{name}
    </select>
    <insert id="insert">
        INSERT INTO BOARD_CT_USER(BOARD_CT_USER_SEQ.NEXTVAL,employeeId, boardCtId)
        VALUES (${id},#{employeeId},${boardCtId})
    </insert>
</mapper>