<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="commute">
    <select id="checkLate" resultType="com.end2end.spring.commute.dto.CommuteDTO">
        SELECT *
        FROM COMMUTE
        WHERE
            state = 'NOT_WORK'
        AND
            TRUNC(regDate) = TRUNC(SYSDATE)
    </select>

    <select id="selectLeaveEarly" resultType="com.end2end.spring.commute.dto.CommuteDTO">
        SELECT *
        FROM COMMUTE
        WHERE STATE = 'WORK_OFF'
        AND EXTRACT(HOUR FROM regDate) = 18
        AND TRUNC(regDate) = TRUNC(SYSDATE)
    </select>

    <insert id="insertAll" parameterType="list">
        INSERT INTO COMMUTE(id, employeeId)
        SELECT COMMUTE_ID_SEQ.NEXTVAL, A.* FROM (
        <foreach collection="list" item="item" separator="UNION ALL">
            SELECT #{item.employeeId} FROM DUAL
        </foreach>
        ) A
    </insert>

    <update id="workOn">
        UPDATE COMMUTE SET state = 'WORK_ON', regDate = SYSDATE WHERE employeeId = #{employeeId} AND TRUNC(regDate) = TRUNC(SYSDATE)  AND state='NOT_WORK'
    </update>

    <insert id="workOff">
        INSERT INTO COMMUTE(state, employeeId) VALUES ('WORK_OFF', #{employeeId})
    </insert>
</mapper>